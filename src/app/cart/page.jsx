import CartPreview from "../../../components-next/CartPreview";

export const metadata = {
  title: "Cart Preview",
  robots: { index: false, follow: false },
};

export default function CartPage() {
  return <CartPreview />;
}
